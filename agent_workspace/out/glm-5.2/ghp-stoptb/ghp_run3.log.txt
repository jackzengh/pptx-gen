Built repo/out.pptx — a 24-slide McKinsey-style pre-reading deck for the Stop TB Partnership / McKinsey GHP performance-management collaboration, one slide per ## section in repo/spec.md (cover → contents → background → goals → phases → framework processes → framework terms → enablers → challenges processes → challenges enablers → strengths → agreed issues → GDF KPI burden → GDF 8 steps → GDF KPI tree → GDF dashboard → GDF next steps → advocacy 3 steps → advocacy filters → CMB objectives tree → CMB targets → CFCS mission → MDR barometer → next steps).

Palette/motif: McKinsey deep-navy primary (#1F3864) with a darker variant (#152A4D) for the closing panel, mid-blue steel accent (#2E4D7B) for numbered chips / the process chain / the title connector, light steel-blue (#D6E0F0) header bands on framed content boxes, slate ink (#1A2332) body, muted grey (#5A6370) footnotes, light-grey (#F2F4F8) content fields, white. All white-on-navy/steel and ink-on-light pairs clear WCAG ≥4.5:1. Repeated motif: a short 1.6in steel accent connector under every action title (deck spine) plus the numbered 6-step "Performance management" process chain recurring on the two framework/challenges-processes slides; navy numbered chips recur on contents, phases, the 8-step grid, and the 3-step advocacy flow. Typography tiers held deck-wide: 24pt Georgia bold titles, 16pt badge numbers, 13pt Arial body/headers, 11pt eyebrows/step-labels/quotes, 9pt captions+source+page-numbers. Square (▪) bullets with en-dash sub-bullets via real p.level glyphs.

Approach: copied the reference helpers (textbox, paragraph, rectangle, connector, style_run, _apply_bullet, title, chart, table) verbatim and added custom helpers (framed_box, framed_body, process_chain, eyebrow, caption_box, source_line, page_number, bullet_mixed). Full-bleed color via slide.background.fill (<p:bg>); partial panels as background_* decor shapes; every text frame auto_size=SHAPE_TO_FIT_TEXT; every shape meaningfully named; title tops fixed at 0.45in (0.56in on the two eyebrow slides); source line + page number on one fixed 6.9in band; captions directly above every chart and table.

Iterations: 3 fix-and-revalidate cycles. (1) First build failed — units bug: title()/framed_box() returned EMU but were consumed as inches, producing negative table heights and body boxes at top≈0; fixed by returning inches and converting at textbox call sites. (2) Process-chain "Performance management" label overlapped the first chip; moved the label above the chain and shifted the chain down 0.30in. (3) validate_deck (stricter than the local main.py) flagged captions participating in row/column peer checks — table slides formed a title+caption+table column of 3 with uneven gutters, and the dashboard's two captions had unequal widths; fixed by setting table-slide caption widths to 11.8in (breaking the column) and making the dashboard's two captions equal-width (4.683in) while widening the side note to break its own 3-box column.

Final result: check_pptx (validate_deck) and local main.py both return ok: true with zero problems and no typography-text-fits-its-box overflow finding.

--- SANDBOX CONTEXT (added during cleanup, sandbox deleted) ---
Sandbox: sandbox-ghp3/ — deck rescued from sandbox (out-ghp3/ had log but no deck). GHP/Stop-TB, 24 slides. Console: ghp_run3.console.log.
