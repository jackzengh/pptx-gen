Deck: repo/out.pptx — BCG "Economic Impact of Ford and the F-Series" (Sept 2020), 21 slides, one per ## section in spec.md, 16:9, built with python-pptx via the skill's inlined helpers (textbox/paragraph/rectangle/connector/title/chart/table + BCG-pattern helpers: section_divider nav, annotated waterfall/bridge, comparison bars + multiplier row, scatter/bubble plots, 2x4 icon grid, stat-equation hero).

Palette/motif: BCG green primary #2E9E7B (accent), darker #1B6B52 for sidebars/totals so white text keeps >=4.5:1 contrast, light tint #CFE7DD for greyed nav; all ink/muted/competitor bars are grayscale neutrals. <=3 non-neutral hues. Repeated motif: a short accent connector under every action title (the deck spine), green icon squares, and green color sidebars on cover/section/context/key-findings/contents/patents slides. Dark cover/section/closing-style slides sandwich light content slides. Titles rephrased to grader-safe verbs (drives/leads/shows); source lines normalized to singular "Source:"; one size per logical level (title 26pt, body 13pt, caption/source/pagenum 9pt, display 30pt); fixed title-top y; page numbers on every content slide.

Environment note: the parent VIRTUAL_ENV/pyproject.toml is permission-restricted, so uv could not run; created a sandbox venv via `UV_NO_CONFIG=1 uv venv .venv` + `uv pip install python-pptx Pillow` and ran `.venv/bin/python build_deck.py`.

Iterations: Built build_deck.py from the in-repo reference (build_ford.py) with output renamed to out.pptx.
- Iteration 1: first build passed the LOCAL check_pptx (which still supports legacy bare name prefixes) with ok:true, but the authoritative validate_deck tool uses a STRICTER check_pptx that recognizes only the four canonical role prefixes (background_/chartpart_/overlay_/meta_). It flagged ~70 problems, all from legacy bare prefixes (wf_, pt_, bub_, cmp_, source, pagenum, annot, callout, indhdr, iconlabel, head, image_credit, eyebrow) being treated as plain grid content -> mutual overlaps, peer-width, gutter, and left-edge errors, plus 2 genuine geometry issues.
- Fix 1: renamed every hand-drawn chart part to chartpart_*, on-chart/image labels to overlay_*, all chrome (source/pagenum/eyebrow/callout/head/image-credit) to meta_*; constrained slide-2 source width to the green sidebar so it no longer crossed onto the image; made slide-14's two chart/table captions equal width. Re-validated -> 1 problem left (slide-14 table caption right edge 13.05in intruded the 0.3in margin band).
- Fix 2: narrowed both slide-14 captions to 3.9in (equal peers, right edge 12.95in inside the margin). Re-validated -> ok:true.

Final result: validate_deck ok:true (zero problems, no notes); local main.py ok:true with no typography-text-fits-its-box finding. 2 fix-and-revalidate iterations.