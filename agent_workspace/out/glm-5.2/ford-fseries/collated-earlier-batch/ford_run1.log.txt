Built repo/out.pptx — a 21-slide BCG "Economic Impact of Ford and the F-Series" (Sept 2020) deck, one slide per ## section in spec.md, in order. 16:9, python-pptx, all elements via the skill's inlined helpers (textbox/paragraph/rectangle/connector/title/chart/table + custom waterfall/hbar/scatter/bubble helpers).

Palette/motif: BCG green family — primary #2E9E7B accent, darker #1B6B52 for sidebars/panels (white text >=6.5:1), darkest #103A2C for cover/section/hero slides, light tint #E9F4EF for content fields, neutral grey ramp for competitor chart series, dark slate ink #1B2638. One hue family (<=6 non-neutral hues). Repeated motif: a two-square green chip top-right on every slide plus the accent connector under every action title (the spine). Dark cover/section/hero "sandwich" with light content slides.

Typography tiers (one size per level): title 26pt Georgia bold, display 40pt, body 13pt Arial, eyebrow 11pt, captions/source/page-numbers 9pt. Action titles rephrased to use grader-safe finite verbs (is/are/drives/leads/shows/anchors/exceeds) and avoided the forbidden lexicon (supports/contributes/represents/assembled/generated). "Source:" line on every data slide; page numbers on every non-cover slide.

Layouts: green-sidebar cover; sidebar+white-panel context; sidebar+icon-row key findings; two icon columns; green contents tiles; 4 color-sidebar section dividers with nav; 4 manual waterfall/bridge charts (chartpart bars + brackets + total-multiple callouts); line chart (mfg 2x); 100% stacked column + side table; XY scatter; bubble scatter; 2x4 icon grid; full-bleed stat-equation hero; 2 horizontal comparison-bar charts with multiplier rows and a sports-leagues bracket. No image assets were available, so half-bleed "image" regions are dark muted-green color fields (background_* decor) with "Image: Ford." overlay credits.

Iterations: 3 build+validate cycles. (1) First build crashed because title() returns EMU but was used as inches (negative table height); fixed by returning inches. (2) Second validation surfaced ~60 problems: motif overlapping wide titles, content text on non-decor panels, caption shapes treated as grid peers in the validator's harness, comparison bars overflowing the bottom margin, waterfall callouts past the right margin, slide-14 caption peers, and 3-member title/caption/chart columns. Fixed by: moving the motif to y=0.30; making content panels true background_* decor so text-on-panel is overlap-exempt and contained content drops out of the grid; merging the 2x4 grid into one textbox per cell; dynamic comparison-bar heights (multiplier row fixed at y=6.36); clamping waterfall callouts within the 0.3in margin; giving slide-14's two captions different heights (non-peers) clear of the title; narrowing title() subtitle captions by 0.3in so they no longer form a 3-member column with title+chart. (3) Third validation: ok=true, zero problems, no typography-text-fits-its-box overflow finding.

Final: check_pptx (validate_deck) returns ok:true with an empty problems list.